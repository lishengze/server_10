cmake_minimum_required(VERSION 2.8)

PROJECT (adk_project)

set(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)
set(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/lib)
#set(CMAKE_CXX_FLAGS "--std=c++11 -fPIC -g3")
#set(CMAKE_CXX_FLAGS "--std=c++11 -fPIC -g3 -m64 -finline-functions -Wno-inline -Wall ")

execute_process (COMMAND arch  OUTPUT_VARIABLE ARCH)
if (ARCH MATCHES "x86_64")
    set(CMAKE_CXX_FLAGS "--std=c++11 -fPIC -g3 -m64 -finline-functions -Wno-inline -Wall ")
    set(librudp src/rudp_engine)
    MESSAGE(STATUS "arch is x86_64")
else ()
    set(CMAKE_CXX_FLAGS "--std=c++11 -fPIC -g3 -finline-functions -Wno-inline -Wall ")
    set(librudp "")
    set(ARM "true")
    MESSAGE(STATUS "arch is arm")
endif ()

set(CMAKE_VERBOSE_MAKEFILE OFF)
#list(APPEND CMAKE_CXX_FLAGS "-lrt")
set(Boost_USE_STATIC_LIBS        OFF)
set(Boost_USE_MULTITHREADED      ON)
set(Boost_USE_STATIC_RUNTIME    OFF)
#find_library(myboost NAMES libboost_regex libboost_thread HINTS ../../3rd/boost_1_62_0/stage/lib)
#find_package(Boost REQUIRED COMPONENTS date_time filesystem iostreams program_options random system thread log locale regex)
find_package(Boost)
if(Boost_FOUND)
#    list(REMOVE_ITEM Boost_LIBRARIES "*libpthread.so")
#    MESSAGE(STATUS ${Boost_LIBRARIES})
#    MESSAGE(STATUS ${myboost})
    file(GLOB_RECURSE adk_SRC src/*.cpp)
    file(GLOB_RECURSE adk_SRC_PACK src_pack/*.cpp)
    if (DEFINED ARM)
        file(GLOB_RECURSE adk_remove src/rudp_engine/*)
        file(GLOB_RECURSE adk_pack_remove src_pack/rudp_engine/*)
        list(REMOVE_ITEM adk_SRC ${adk_remove})
        list(REMOVE_ITEM adk_SRC_PACK ${adk_pack_remove})
    endif ()
    file(GLOB_RECURSE adk_HDR include/*.h)
    include_directories(${Boost_INCLUDE_DIRS} include)
    LINK_DIRECTORIES($ENV{BOOST_ROOT}/stage/lib)

IF(DEFINED ENV{BUILD_AMI})
MESSAGE(STATUS "libadk is static linked by libami")
ELSE()
    include_directories(../../3rd/libssh/include include/adk/rdma src/io_engine/tcp_verbs ../../3rd ${librudp})
#    ADD_LIBRARY(adk SHARED ${adk_SRC} ${adk_HDR})
    ADD_LIBRARY(adk STATIC ${adk_SRC} ${adk_HDR})
    set_property(TARGET adk PROPERTY POSITION_INDEPENDENT_CODE OFF)
#    set(CMAKE_STATIC_LINK_FLAGS "-Wl,--whole-archive")
    set(myboost_regex -Wl,--whole-archive boost_regex.a -Wl,--no-whole-archive)
    set(myboost_date_time -Wl,--whole-archive boost_date_time.a -Wl,--no-whole-archive)
    set(myboost_filesystem -Wl,--whole-archive boost_filesystem.a -Wl,--no-whole-archive)
    set(myboost_program_options -Wl,--whole-archive boost_program_options.a -Wl,--no-whole-archive)
    set(myboost_log -Wl,--whole-archive boost_log.a -Wl,--no-whole-archive)
    set(myboost_locale -Wl,--whole-archive boost_locale.a -Wl,--no-whole-archive)
    set(myboost_system -Wl,--whole-archive boost_system.a -Wl,--no-whole-archive)
    set(myboost_thread -Wl,--whole-archive boost_thread.a -Wl,--no-whole-archive)
    set(myboost_random -Wl,--whole-archive boost_random.a -Wl,--no-whole-archive)
    target_link_libraries(adk ${myboost_regex})
    target_link_libraries(adk ${myboost_date_time})
    target_link_libraries(adk ${myboost_filesystem})
    target_link_libraries(adk ${myboost_program_options})
    target_link_libraries(adk ${myboost_log})
    target_link_libraries(adk ${myboost_locale})
    target_link_libraries(adk ${myboost_system})
    target_link_libraries(adk ${myboost_thread})
    target_link_libraries(adk ${myboost_random})
    target_link_libraries(adk rt)
    target_link_libraries(adk pthread)
    target_link_libraries(adk ssh)
    SET_TARGET_PROPERTIES(adk PROPERTIES CLEAN_DIRECT_OUTPUT 1)
#    ADD_SUBDIRECTORY(test)
endif()
    include_directories(../../3rd/libssh/include include/adk/rdma src/io_engine/tcp_verbs ../../3rd ${librudp})
    ADD_LIBRARY(adk_static STATIC ${adk_SRC} ${adk_SRC_PACK} ${adk_HDR})
    target_link_libraries(adk_static rt)
    target_link_libraries(adk_static pthread)
    target_link_libraries(adk_static ssh)
    SET_TARGET_PROPERTIES(adk_static PROPERTIES OUTPUT_NAME "adk")
    SET_TARGET_PROPERTIES(adk_static PROPERTIES CLEAN_DIRECT_OUTPUT 1)
#    set_property(TARGET adk_static PROPERTY POSITION_INDEPENDENT_CODE ON)
    INSTALL(TARGETS adk_static DESTINATION .)
    set_property(TARGET adk_static PROPERTY POSITION_INDEPENDENT_CODE OFF)
endif()
